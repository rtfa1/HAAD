# echo "[MOCK OUTPUT] Executing Agent with Prompt Length: ${#HAAD_PROMPT} chars."
# To use Docker, replace with:
# docker run --rm -e PROMPT="$HAAD_PROMPT" my-agent-image /bin/run_agent.sh
# opencode run "$HAAD_PROMPT" --model opencode/grok-code